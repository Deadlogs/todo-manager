{
  "name": "todo-manager",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest",
    "test:unit": "jest tests/unit",
    "test:api": "jest tests/api",
    "test:frontend": "npx playwright test",
    "test:all": "npm run test:unit && npm run test:api && npm run test:frontend",
    "coverage:backend": "jest --coverage",
    "coverage:frontend": "npx playwright test && node scripts/generate-coverage.js"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.1.0",
    "multer": "^2.0.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "jest": "^30.2.0",
    "nyc": "^17.1.0",
    "supertest": "^7.2.2",
    "v8-to-istanbul": "^9.3.0"
  }
}
